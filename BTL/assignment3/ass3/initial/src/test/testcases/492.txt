
        void main()
        {
            int year;
            printf("Enter a year: ");
            if(year%4 == 0)
            {
                if( year%100 == 0)
                {
                    // year is divisible by 400, hence the year is a leap year
                    if ( year%400 == 0)
                        print(year);
                    else
                        print(year);
                }
                else
                    print(year);
            }
            else
                print(year);
            
            return 0;
        }
        void print(int a){}
        void printf(string a){}
        
        